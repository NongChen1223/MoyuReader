# 墨鱼阅读器迁移剩余任务清单

更新时间：2026-06-02

本文档只记录从 `Wails` 迁移到 `Electron + React + Go` 过程中，当前仍未彻底完成的工作。

状态约定：

- `未做`：还没有开始，或只停留在思路层
- `做到一半`：已经有实现，但功能、结构或体验还没完全收口
- `已完成待验证`：代码和构建层已完成，但还缺完整桌面实机验收

## 1. 桌面浮窗还原

### 1.1 Electron 浮窗行为对齐旧版

状态：`做到一半`

已完成：

- 已接入独立透明浮窗窗口
- 已接入正文同步
- 已接入章节切换
- 已接入目录选择
- 已接入透明度调节
- 已接入当前章节进度滑动
- 已接入阅读位置回传
- 已接入悬停显隐控件
- 已接入章节边界滚轮切章冷却

未完成：

- 浮窗拖拽与缩放手感继续对齐旧版
- 焦点切换时的显隐细节继续对齐
- 主窗口与浮窗来回切换时的状态恢复继续核对
- 大章节、富文本、图片密集内容下的稳定性继续验证
- 浮窗关闭、隐藏、再次打开的边界行为继续核对

### 1.2 浮窗正文渲染与旧版视觉对齐

状态：`做到一半`

未完成：

- EPUB 图片显示效果继续和旧版比对
- 富文本段落间距、图片约束、章节衔接样式继续比对
- PDF 图片型页面在浮窗中的显示表现继续验证

## 2. 阅读状态一致性

### 2.1 普通阅读模式与浮窗模式进度同步

状态：`做到一半`

已完成：

- 已有阅读位置回传
- 已有当前章节进度同步基础
- 已有浮窗退出后回写主阅读区的基础流程

未完成：

- 普通模式 -> 浮窗 -> 普通模式的完整一致性验收
- 章节切换后两边进度一致性继续验证
- 拖动进度条后两边同步继续验证
- 搜索跳转后的同步继续验证

### 2.2 普通模式与浮窗模式交互一致性

状态：`做到一半`

未完成：

- 目录切章在两种模式下的行为继续逐项核对
- 章节边界滚动体验继续核对
- 透明度调整后的视觉一致性继续核对

## 3. 旧桌面壳耦合清理

### 3.1 后端服务对旧桌面运行时的直接依赖

状态：`做到一半`

已完成：

- `NovelService` 的文件选择已改为可注入选择器
- `App` 的目录选择已改为可注入选择器

未完成：

- `backend/services/window_service.go` 仍保留旧桌面运行时窗口 API
- `backend/app/app.go` 仍保留旧运行时事件发送路径
- 继续判断哪些旧桌面壳 API 可以下沉到兼容层，哪些应直接删除

### 3.2 旧桌面入口文件清理

状态：`未做`

未完成：

- 评估 [main.go](./main.go) 是否还能下线
- 评估 [wails.json](./wails.json) 是否还能下线
- 评估 `go.mod` 中 Wails 相关依赖何时移除
- 明确旧入口保留到哪个阶段结束

## 4. Electron 工程收口

### 4.1 默认工作流继续收口

状态：`做到一半`

已完成：

- 根目录已有 `pnpm dev`
- 根目录已有 `pnpm build`
- 文档默认入口已切到 Electron

未完成：

- 继续减少默认文档中对旧桌面入口的显式暴露
- 继续清理迁移期兼容命名和注释

### 4.2 Electron 打包链路

状态：`未做`

未完成：

- 选定正式打包方案
- 接入安装包或分发构建脚本
- 明确构建产物目录与发布流程

## 5. 命名与兼容层整理

### 5.1 前端历史兼容命名

状态：`做到一半`

未完成：

- `useFixWailsDrag` 仍作为底层兼容实现保留
- `--wails-draggable` 仍作为历史样式变量存在
- 继续判断哪些命名只需要注释解释，哪些值得真正重命名

## 6. 验证与验收

### 6.1 当前已通过的验证

状态：`已完成待验证`

已完成：

- 前端类型检查通过
- 前端构建通过
- Go API 服务构建通过
- 全仓 Go 构建通过
- Electron 主进程脚本语法检查通过

### 6.2 仍缺的验证

状态：`未做`

未完成：

- 当前环境下完整跑起 Electron 桌面端并逐项手测
- 对照旧版本逐项验收功能
- 重点验收摸鱼浮窗、进度同步、章节切换、富文本和图片场景

## 7. 文档最终收口

### 7.1 迁移完成后的最终文档版

状态：`未做`

未完成：

- 迁移全部完成后，再压缩 README 里的过渡描述
- 迁移全部完成后，再更新需求文档里的“迁移中”表述
- 形成最终默认桌面方案说明
